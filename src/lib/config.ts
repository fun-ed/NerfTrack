// Public release metadata is intentionally limited to the repository URL. The
// updater accepts only HTTPS GitHub repository URLs and GitHub-hosted release
// assets returned by that repository's latest-release API.
export const GITHUB_REPOSITORY_URL = 'https://github.com/fun-ed/NerfTrack';
export const SHARE_GRAPH_DISCUSSION_URL = `${GITHUB_REPOSITORY_URL}/discussions/categories/-share-your-graph`;
